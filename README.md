# Practice

In order to build this, go into the ray directory and if you are not using windows powershell this will not work, but if you are just run the ./make_n_build_ray.ps1

otherwise just 
```
cmake -B build
cmake --build build
./build/Debug/Ray.exe > image.ppm
```
